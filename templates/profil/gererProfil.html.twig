{% extends 'base.html.twig' %}

{% block title %}Gestion du profil{% endblock %}

{% block main %}
    <h1 class="text-center">Gérer le profil</h1>

    <div class="row d-flex justify-content-center">
        <div class="col-4">
            {{ form_start(participantForm) }}
            {{ form_widget(participantForm) }}
            <button class="btn btn-primary" type="submit">Modifier</button>
{#            <a href="{{ path("profil_afficher", {"id": app.user.id}) }}" class="btn btn-info">Modifier</a>#}
            <a href="{{ path("sorties_home") }}" class="btn btn-info">Annuler</a>
            {{ form_end(participantForm) }}
        </div>
    </div>

{% endblock %}
